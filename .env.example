# Django
SECRET_KEY=replace-with-a-secure-secret-key

# MySQL
DB_NAME=contactflow
DB_USER=django
DB_PASSWORD=django_password
DB_HOST=db
DB_PORT=3306

# User and group IDs for file permissions in the container
UID=replace-with-desired-uid
GID=replace-with-desired-gid